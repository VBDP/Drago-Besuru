The only reason this script exists is because the shaders object is so large that the controller isn't visible in the prefab preview. This script sets the scale of said object then commits non existance. Deal with it.